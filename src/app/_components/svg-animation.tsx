import { FC } from "react"

interface SvgAnimationProps {
  className?: string
  scrollValue: number
}

const SvgAnimation: FC<SvgAnimationProps> = ({}) => {
  return <h1></h1>
}

export default SvgAnimation
